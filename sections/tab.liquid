<style>
.custom-tabs-section{
  width:100%;
  padding:0px 0;
}

{% comment %} .custom-tabs-wrapper{
  max-width:1200px;
  margin:0 auto;
} {% endcomment %}

.custom-tabs-heading{
  margin:0 0 30px;
  text-align:center;
  font-size:36px;
  line-height:1.2;
  color:{{ section.settings.heading_color }};
}

.custom-tabs-row {
    display: flex;
    gap: 2px;
    margin-bottom: 0;
    justify-content: flex-start;
    overflow-x: auto;
    overflow-y: hidden;
    white-space: nowrap;
    scrollbar-width: none;
    -ms-overflow-style: none;
    cursor: grab;
    user-select: none;
    padding: 0px 25px;
    margin-top: 10px;
}
button.custom-tab-button:hover {
    text-decoration: underline;
    text-underline-offset: 17px;
}
.custom-tab-button{
  padding:14px 18px;
  cursor:pointer;
  transition:all .35s ease;
  background:{{ section.settings.tab_bg }};
  color:{{ section.settings.tab_text }};
  border:1px solid {{ section.settings.tab_border }};
  font-size:16px;
 
}

.custom-tab-button.active{
  background:{{ section.settings.active_tab_bg }};
  color:{{ section.settings.active_tab_text }};
  border-color:{{ section.settings.active_tab_bg }};
    color: #ffffff;
    {% comment %} border-color: rgba(0, 0, 0, 0); {% endcomment %}
     border-color: rgb(120 72 158);
    text-decoration: underline;
    text-underline-offset: 17px;
    border-radius: 10px;
    {% comment %} background: linear-gradient(180deg, #6D43A4 0%, #5E3591 100%); {% endcomment %}
     background: #492E5A;
    padding: 12px 16px;
   text-decoration-color: #DFB4F5!important;

}

.custom-tab-content{
  display:none;
  background:{{ section.settings.content_bg }};
  border:1px solid {{ section.settings.content_border }};
  border-radius:18px;
  padding:24px;
  opacity:0;
  transform:translateY(10px);
  transition:opacity .4s ease, transform .4s ease;
}

.custom-tab-content.active{
  display:block;
  opacity:1;
  transform:translateY(0);
}

.custom-tab-content.active{
  opacity:1;
  max-height:1000px;
  padding:0px;
  margin-top:1px;
}
.para-add {
    max-width: 890px;
    margin: auto;
    border-radius: 15px;
  background: #32183F;
    border: 2px solid #78489E;
}
.custom-tab-inner{
  color:{{ section.settings.content_text }};
  font-size:17px;
  line-height:1.7;
}

.custom-tab-inner p:last-child{
  margin-bottom:0;
}
.custom-tab-inner {
    padding: 0px 25px;
    padding-top: 30px;
    padding-bottom: 30px;
}
.custom-tabs-row {
    display: flex;
    gap: 0px;
    margin-bottom: 0;
    justify-content: flex-start;
    overflow-x: auto;
    overflow-y: hidden;
    white-space: nowrap;
    scrollbar-width: none;
    -ms-overflow-style: none;
    cursor: grab;
    user-select: none;
}

.custom-tabs-row::-webkit-scrollbar {
    display: none;
}

.custom-tabs-row:active {
    cursor: grabbing;
}
.one-bord {
    border: 1px solid #78489e;
    margin-top: 8px !important;
    max-width: 837px;
    margin: auto;
}
span.tls-tag {
    font-size: 14px;
}
@media screen and (max-width:768px){

  .custom-tabs-section{
    padding:30px 0;
  }

  .custom-tabs-heading{
    font-size:28px;
    margin-bottom:24px;
  }

  .custom-tabs-row{
    gap:10px;
  }
.custom-tab-inner {
    padding: 0px 0px;
    padding-top: 0px;
    padding-bottom: 0px;
}
  .custom-tab-button{
    width:100%;
    text-align:center;
    padding:12px 10px;
    font-size:13px;
    font-weight: 100;
    {% comment %} text-decoration: underline!important;
    text-underline-offset: 5px !important; {% endcomment %}

  }
.custom-tabs-row{
    padding: 0px 10px;
}
  .custom-tab-content.active{
    padding:18px 0;
  }

  .custom-tab-inner{
    font-size:15px;
  }
.custom-tabs-row {
    display: flex;
    gap: 0px;
    margin-bottom: 0px;
    justify-content: flex-start;
    overflow-x: auto;
    overflow-y: hidden;
    white-space: nowrap;
    scrollbar-width: none;
    
}
.custom-tab-button.active{
    text-decoration: underline;
    text-underline-offset: 15px!important;
    border-radius: 10px;
    background: #4d2174;
    padding: 5px 13px;
    text-decoration-color: #DFB4F5!important;


}
.custom-tabs-heading {
    padding: 3px 8px;
    text-align: start;

}
span.tls-tag {
    padding: 5px 10px;
    border-radius: 20px;
    font-size: 14px;
    margin-right: 5px;
}
.para-add {
    margin: 10px;
}
.tls-vendor {
    color: inherit;
    text-decoration: none;
    font-weight: 500;
    transition: 0.3s ease;
    padding-left: 7px;
    padding-right: 7px;
}
.custom-tabs-wrapper {
    padding: 10px;
}


}
@media screen and (max-width:768px){
  .one-bord{
    border: none;
  }

  .one-bord.active{
    border: 2px solid #78489e;
    border-bottom: none;
  }
      .para-add {
        margin: 10px;
        display: none;
    }
.buy-box.container {
    padding-top: 5px;
    padding-bottom: 15px;
    padding-left: 15px;
    padding-right: 15px;
}

}



.mobile-accordion{
  display:none;
}

@media screen and (max-width:768px){

  .custom-tabs-row,
  .one-bord,
  .custom-tab-content{
    display:none !important;
  }

  .mobile-accordion{
    display:block;
    background: #361641;
    border-radius: 10px;
    border: 1px solid rgb(120 72 158 / 58%);
  }

  .mobile-accordion-item{
  border-bottom: 1px solid rgb(120 72 158 / 58%);
  }

  .mobile-accordion-btn{
    width:100%;
    background:none;
    border:none;
    color:#fff;
    text-align:left;
    padding:20px;
    font-size:16px;
    cursor:pointer;
    display:flex;
    justify-content:space-between;
    align-items:center;
  }

  .accordion-icon{
    font-size:32px;
    line-height:1;
  }

  .mobile-accordion-content{
    display:none;
    padding:0 20px 20px;
  }

  .mobile-accordion-item.active .mobile-accordion-content{
    display:block;
  }

  .mobile-accordion-item.active .accordion-icon{
    content:"-";
  }
}




</style>

<div class="custom-tabs-section">
  <div class="custom-tabs-wrapper">

    {% if section.settings.heading != blank %}
      <h2 class="custom-tabs-heading">
        {{ section.settings.heading }}
      </h2>
    {% endif %}

<div class="para-add">
    <div class="custom-tabs-row">
      {% for block in section.blocks %}
        <button 
          class="custom-tab-button {% if forloop.first %}active{% endif %}" 
          data-tab="tab-{{ block.id }}"
          {{ block.shopify_attributes }}
        >
          {{ block.settings.tab_title }}
        </button>
      {% endfor %}
    </div>
<div class="one-bord"></div>
    {% for block in section.blocks %}
      <div 
        class="custom-tab-content {% if forloop.first %}active{% endif %}" 
        id="tab-{{ block.id }}"
      >
        <div class="custom-tab-inner">
          {{ block.settings.tab_content }}
        </div>
      </div>
    {% endfor %}

</div>

<div class="mobile-accordion">

  {% for block in section.blocks %}
    <div class="mobile-accordion-item">

      <button class="mobile-accordion-btn">
        {{ block.settings.tab_title }}
        <span class="accordion-icon">+</span>
      </button>

      <div class="mobile-accordion-content">
        <div class="custom-tab-inner">
          {{ block.settings.tab_content }}
        </div>
      </div>

    </div>
  {% endfor %}

</div>

  </div>
</div>

<script>
document.addEventListener('DOMContentLoaded', function(){

  const buttons = document.querySelectorAll('.custom-tab-button');
  const contents = document.querySelectorAll('.custom-tab-content');
  const borderLine = document.querySelector('.one-bord');

  // Mobile par sab closed
  if (window.innerWidth <= 768) {
    buttons.forEach(btn => btn.classList.remove('active'));
    contents.forEach(content => content.classList.remove('active'));

    if (borderLine) {
      borderLine.classList.remove('active');
    }
  }

  buttons.forEach((button) => {

    button.addEventListener('click', function(){

      const target = document.getElementById(this.getAttribute('data-tab'));

      // Mobile
      if (window.innerWidth <= 768) {

        const isActive = this.classList.contains('active');

        // Sab close
        buttons.forEach(btn => btn.classList.remove('active'));
        contents.forEach(content => content.classList.remove('active'));

        if (borderLine) {
          borderLine.classList.remove('active');
        }

        // Agar closed tha to open karo
        if (!isActive) {
          this.classList.add('active');
          target.classList.add('active');

          if (borderLine) {
            borderLine.classList.add('active');
          }
        }

      } else {

        // Desktop normal tabs
        buttons.forEach(btn => btn.classList.remove('active'));
        contents.forEach(content => content.classList.remove('active'));

        this.classList.add('active');
        target.classList.add('active');

        if (borderLine) {
          borderLine.classList.add('active');
        }
      }

    });

  });

});
</script>

<script>
const slider = document.querySelector('.custom-tabs-row');

let isDown = false;
let startX;
let scrollLeft;

slider.addEventListener('mousedown', (e) => {
  isDown = true;
  slider.classList.add('active');
  startX = e.pageX - slider.offsetLeft;
  scrollLeft = slider.scrollLeft;
});

slider.addEventListener('mouseleave', () => {
  isDown = false;
});

slider.addEventListener('mouseup', () => {
  isDown = false;
});

slider.addEventListener('mousemove', (e) => {
  if (!isDown) return;
  e.preventDefault();
  const x = e.pageX - slider.offsetLeft;
  const walk = (x - startX) * 2;
  slider.scrollLeft = scrollLeft - walk;
});

</script>



<script>
document.addEventListener('DOMContentLoaded', function(){

  const accordionBtns = document.querySelectorAll('.mobile-accordion-btn');

  accordionBtns.forEach(btn => {

    btn.addEventListener('click', function(){

      const item = this.parentElement;
      const icon = this.querySelector('.accordion-icon');

      document.querySelectorAll('.mobile-accordion-item').forEach(acc => {
        if(acc !== item){
          acc.classList.remove('active');
          acc.querySelector('.accordion-icon').textContent = '+';
        }
      });

      item.classList.toggle('active');

      if(item.classList.contains('active')){
        icon.textContent = '−';
      }else{
        icon.textContent = '+';
      }

    });

  });

});
</script>

{% schema %}
{
  "name": "Custom Tabs Section",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Section Heading",
      "default": "Product Information"
    },
    {
      "type": "color",
      "id": "heading_color",
      "label": "Heading Color",
      "default": "#111111"
    },
    {
      "type": "color",
      "id": "tab_bg",
      "label": "Tab Background",
      "default": "#f4f4f4"
    },
    {
      "type": "color",
      "id": "tab_text",
      "label": "Tab Text Color",
      "default": "#111111"
    },
    {
      "type": "color",
      "id": "tab_border",
      "label": "Tab Border Color",
      "default": "#dddddd"
    },
    {
      "type": "color",
      "id": "active_tab_text",
      "label": "Active Tab Text",
      "default": "#ffffff"
    },
    {
      "type": "color",
      "id": "content_bg",
      "label": "Content Background",
      "default": "#ffffff"
    },
    {
      "type": "color",
      "id": "content_border",
      "label": "Content Border",
      "default": "#e5e5e5"
    },
    {
      "type": "color",
      "id": "content_text",
      "label": "Content Text",
      "default": "#444444"
    }
  ],
  "blocks": [
    {
      "type": "tab",
      "name": "Tab",
      "settings": [
        {
          "type": "text",
          "id": "tab_title",
          "label": "Tab Title",
          "default": "Tab Title"
        },
        {
          "type": "richtext",
          "id": "tab_content",
          "label": "Tab Content",
          "default": "<p>Add your tab content here.</p>"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Custom Tabs Section",
      "blocks": [
        {
          "type": "tab"
        },
        {
          "type": "tab"
        },
        {
          "type": "tab"
        }
      ]
    }
  ]
}
{% endschema %}