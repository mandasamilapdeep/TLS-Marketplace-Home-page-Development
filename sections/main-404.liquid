{{ 'template-collection.css' | asset_url | stylesheet_tag }}
{{ 'component-article-card.css' | asset_url | stylesheet_tag }}
{{ 'component-card.css' | asset_url | stylesheet_tag }}

{{ 'component-search.css' | asset_url | stylesheet_tag }}
{{ 'template-search.css' | asset_url | stylesheet_tag }}

{%- if settings.quickview_show
	or settings.show_on_mobile
	or settings.type_color_swatch == 'custom'
-%}
	{{ 'section-main-product.css' | asset_url | stylesheet_tag }}
{% endif %}

{%- style -%}
	{% render 'section-margin' %}
{%- endstyle -%}

<script src="{{ 'icon-cancel.js' | asset_url }}" defer="defer"></script>

{%- if settings.quickview_show or settings.show_on_mobile -%}
	<link
		rel="stylesheet"
		href="{{ 'component-deferred-media.css' | asset_url }}"
		media="all"
	>
	<link
		rel="preload"
		href="{{ 'quick-add.css' | asset_url }}"
		as="style"
		onload="this.onload=null;this.rel='stylesheet'"
	>
{%- endif -%}

<div class="template-404">
	<div class="container">
		<div class="page-header">
			<div class="page-header__inner">
				<div class="neon-404-header">
					<div class="svg-blur">
						{% render 'icon-circle-blur' %}
					</div>
					<span class="error-code">404</span>
				</div>
				<h1 class="main-page-title page-title title--page h2">
					{{ 'templates.404.title' | t }}
				</h1>
				<p class="search__form-text large-font">
					{{ 'templates.404.text' | t }}
				</p>
			</div>
		</div>
		{% comment %}
			{%- if settings.predictive_search_enabled -%}
			  <predictive-search data-loading-text="{{ 'accessibility.loading' | t }}">
			{%- endif -%}
		{% endcomment %}
		{%- if search.performed
			and search.results_count == 0
			and search.filters == empty
		-%}
			<div class="search-wrapper">
		{% endif %}
		<form
			action="{{ routes.search_url }}"
			method="get"
			role="search"
			class="search"
		>
			<div class="field">
				<button
					class="search__button button focus-inset"
					aria-label="{{ 'general.search.search' | t }}"
				>
					{% render 'icon-search' %}
				</button>
				<input
					dir="ltr"
					class="search__input field__input focus-inset"
					id="Search-In-Template"
					type="search"
					name="q"
					value="{{ search.terms | escape }}"
					placeholder="{{ 'general.search.placeholder' | t }}"
					tabindex="0"
				>
				<label class="visually-hidden" for="Search-In-404">
					{{- 'general.search.search' | t -}}
				</label>
				<input name="options[prefix]" type="hidden" value="last">

				<div class="search-buttons-wrapper">
					<span
						class="button reset__button field__button"
						aria-label="{{ 'general.search.reset' | t }}"
					>
						{% render 'icon-cancel' %}
					</span>

					<div class="button_wrapper custom-hover">
						<button
							class="search__button--primary button focus-inset"
							aria-label="{{ 'general.search.search' | t }}"
							tabindex="0"
						>
							<div class="button__content">
								<span
									class="button__label"
									data-label="{{- 'general.search.search' | t | escape -}}"
								>
									{{ 'general.search.search' | t }}
								</span>
							</div>
							<span class="button_circle"> </span>
						</button>
					</div>
				</div>
			</div>
			{% comment %}
				{%- if settings.predictive_search_enabled -%}
				  <div class="predictive-search predictive-search--search-template" tabindex="-1" data-predictive-search>
				    <div class="predictive-search__loading-state">
				      {% render 'icon-spinner' %}
				    </div>
				  </div>
				  <span class="predictive-search-status visually-hidden" role="status" aria-hidden="true"></span>
				{%- endif -%}
			{% endcomment %}
		</form>
		{%- if search.performed
			and search.results_count == 0
			and search.filters == empty
		-%}
			</div>
		{% endif %}
		{% comment %}
			{%- if settings.predictive_search_enabled -%}
			  </predictive-search>
			{%- endif -%}
		{% endcomment %}
	</div>
</div>

{% schema %}
{
	"class": "spaced-section",
	"settings": [
		{
			"type": "header",
			"content": "t:sections.all.section-margin.header.content"
		},
		{
			"type": "select",
			"id": "margin_top",
			"options": [
				{
					"value": "no-indent",
					"label": "t:sections.all.section-margin.options__1.label"
				},
				{
					"value": "xs",
					"label": "t:sections.all.section-margin.options__2.label"
				},
				{
					"value": "s",
					"label": "t:sections.all.section-margin.options__3.label"
				},
				{
					"value": "m",
					"label": "t:sections.all.section-margin.options__4.label"
				},
				{
					"value": "l",
					"label": "t:sections.all.section-margin.options__5.label"
				},
				{
					"value": "xl",
					"label": "t:sections.all.section-margin.options__6.label"
				}
			],
			"default": "no-indent",
			"label": "t:sections.all.section-margin.margin_top.label"
		},
		{
			"type": "select",
			"id": "margin_bottom",
			"options": [
				{
					"value": "no-indent",
					"label": "t:sections.all.section-margin.options__1.label"
				},
				{
					"value": "xs",
					"label": "t:sections.all.section-margin.options__2.label"
				},
				{
					"value": "s",
					"label": "t:sections.all.section-margin.options__3.label"
				},
				{
					"value": "m",
					"label": "t:sections.all.section-margin.options__4.label"
				},
				{
					"value": "l",
					"label": "t:sections.all.section-margin.options__5.label"
				},
				{
					"value": "xl",
					"label": "t:sections.all.section-margin.options__6.label"
				}
			],
			"default": "l",
			"label": "t:sections.all.section-margin.margin_bottom.label"
		}
	]
}
{% endschema %}
